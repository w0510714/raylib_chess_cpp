#include <raylib.h>
#include "chess_analysis_program.h"

int main() {
    // Create and run the chess analysis program
    ChessAnalysisProgram app;

    // Everything should be taken care of elsewhere
    app.run();    
    return 0;
}